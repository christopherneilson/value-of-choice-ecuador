# OS / editor noise
.DS_Store
Thumbs.db
.Rhistory
*.swp

# Build caches / private intermediates (the appendix HTML is the only built artifact we commit)
_cache.pkl
*.pkl
.env
.env.*

# engine test outputs (regenerated by node engine/test_engine.mjs)
engine/fixtures/js_sic_*.json
